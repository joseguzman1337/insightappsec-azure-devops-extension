{
    "description": "The package.json for unit tests.",
    "devDependencies": {
        "@types/jest": "30.0.0",
        "jest": "30.3.0",
        "ts-jest": "29.4.6",
        "typescript": "5.9.3"
    },
    "scripts": {
        "test": "jest",
        "coverage": "jest --coverage",
        "build": "cd tasks/InsightAppSec && npm install && npx tsc task.ts --lib ES2017 && cd ../.. && tfx extension create --manifest-globs vss-extension.json"
    }
}
